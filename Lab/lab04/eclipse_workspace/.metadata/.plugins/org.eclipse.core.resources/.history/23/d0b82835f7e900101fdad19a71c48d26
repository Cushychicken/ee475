/*
 * lcdwrite.c
 *
 *  Created on: Sep 28, 2011
 *      Author: nash.reilly
 */

#include <stdio.h>
#include <string.h>
#include "C:\NRCREE475\lab04\DE2_System\nios_qsys\software\lcd\lcdwrite.h"

#define Switches (volatile int *) 0x01001010
#define LEDs 	 (int *) 0x01001000\

int main (void){

	int test 		= 0;
	int prev 		= 0;
	int VERBOSE 	= 0;
	int j,k;
	int test_array  = {1,2,4,8};
	char str[] = "Initialize.";

	sprintf(str, "Switch program!");
	WriteLCD(str);

	while(1){
		test = *Switches;

		if(VERBOSE == 1){
			printf("Current value of test: %d\n", test);
		};

		switch(test){
			case '1':
				WriteLCD(sw1); break;
			case '2':
				WriteLCD(sw2); break;
			case '4':
				WriteLCD(sw3); break;
			case '8':
				WriteLCD(sw4); break;
		}
		prev = test;

	};

	return(1);

}
